{
  "name": "simple-analytics",
  "version": "1.0.0",
  "description": "Simple analytics tracking service",
  "main": "server.js",
  "scripts": {
    "start": "npm run migrate && node server.js",
    "dev": "npm run migrate && nodemon server.js",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}